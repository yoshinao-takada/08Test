#include "BMFSM.h"
#include "BMTest.h"

BMFSM_SDECL(sfsm);

BMStatus_t BMFSMUT()
{
    BMStatus_t status = BMStatus_SUCCESS;
    do {

    } while (0);
    BMTest_ENDFUNC(status);
    return status;
}